# Scratchbook Population — Pacing Plan (template)

> Blank session-chunking plan for a large-corpus Scratchbook population. Fill once, near the start
> of population, once the corpus is known (source list, clusters, fulltext-registration status).
> Companion to `entry-prompt-templates.md` (the *what to paste*) and the project's own progress
> tracker (the *live status*) — this file is the *how to sequence it*.

---

## Why this needs a plan, not just templates

[State the corpus size and why a single sweep is a risk here, e.g. "N papers, M already
fulltext-registered — a single-turn sweep of M+ full papers degrades contradiction-checking
accuracy and turns `Argumen saat ini` refresh into an end-of-batch afterthought instead of a
per-edit discipline."]

---

## Chunking axes used for this project

[List which of the skill's three axes apply, and how — source cost, corpus cluster, sub-camp —
plus any project-specific axis, e.g. a data-availability split.]

---

## Recommended session sequence

| # | Session focus | Template(s) | Papers/scope | Why this order |
|---|---|---|---|---|
| 1 | | | | |

**Batch-size ceiling:** [default 5 fulltext papers/session — override here if a different ceiling
fits this corpus better, and say why]

---

## Session-boundary checklist

End every fulltext-processing session with:
1. Density audit, scoped to the section(s) touched this session
2. Update the progress tracker — mark each processed key's status, note contradictions, note
   sections touched
3. A one-line note of exactly which cluster/keys remain, for the next session's cold start

---

## What NOT to do

- Don't invoke a "sweep everything" template literally as a single-turn command — it's a checklist
  reference for this table's batches
- Don't interleave synthesis sections (Future Work, Conclusion) with fulltext sweeping
- [Add any project-specific late-pass rule, e.g. a targeted cross-cluster contradiction re-check
  once known overlap pairs are identified]

## Change log
- [date] — created from `ARTi-scratchbook`'s pacing-plan-template.md.
