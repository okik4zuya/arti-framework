# Scratchbook Population — Progress Tracker (template)

> Per-source checkpoint tracker — the single source of truth for "which source has and hasn't been
> populated" so this never requires re-reading the Scratchbook itself. Read first when resuming a
> population session. Update at every session boundary (see pacing plan) **and** immediately after
> every individual extraction — one key swept = one row edit, same turn, never deferred.

**Session sequence reference:** [link to this project's pacing plan]

## [Cluster / group name]

| Key | Fulltext? | Planned session | Status | Sections touched | Contradiction flagged? |
|---|---|---|---|---|---|
| | | | ⬜ | | |

**Status vocabulary (exhaustive — no row may be ambiguous):**
- ⬜ not started
- ✅ swept
- ⚠️ swept with contradiction flagged
- ❌ export-only / scavenger-citation only (abstract-only, never gets a full extraction pass)

## Totals
- Not started:
- Swept:
- Export-only:

## Change log
- [date] — tracker created from `ARTi-scratchbook`'s progress-tracker-template.md, seeded from
  [corpus source, e.g. a research-design.md Corpus Definition table].
