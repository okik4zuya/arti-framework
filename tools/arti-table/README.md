# arti-table (stub)

Table lane for ARTi-figure. Not implemented yet - deferred until the diagram
lane (`arti-render`) is stable and there's a concrete table need. When built,
it should follow the same pattern as `arti-render`: a JSON spec in, an
editable output (likely `.drawio` or `.docx` table markup) out, pure stdlib
where possible.
