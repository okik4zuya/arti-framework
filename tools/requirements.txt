# Dependencies for ~/.arti tools, installed into the vendored interpreter.
# arti-render needs only stdlib.
python-docx

# arti-plot: per-project plot scripts (see arti-plot/README.md) render with
# matplotlib. No shared render.py yet -- each script imports this directly.
matplotlib

# arti-pdf-ingest: PDF text extraction + OCR fallback (pairs with the
# poppler/tesseract binaries vendored into ~/.arti/bin by the NSIS installer).
pdfminer.six==20250506
pdf2image==1.17.0
pytesseract==0.3.13

# dashboard chat feature (ported from fine-ai-chat): OpenAI-compatible client
# + token estimation for the context-curation chat panel.
openai
tiktoken
