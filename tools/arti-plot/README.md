# arti-plot (stub)

Plot lane for ARTi-figure. Lowest priority - no plot data exists yet in any
active project. When built, this is the one lane expected to need a
dependency beyond stdlib (likely `matplotlib`); add it to `requirements.txt`
at that point, not before.
